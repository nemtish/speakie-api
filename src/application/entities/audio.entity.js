
export default class Audio {
    constructor (
        hashUrl,
        binary,
        duration,
        dateTime
    ) {
        this.hashUrl = hashUrl;
        this.binary = binary;
        this.duration = duration;
        this.dateTime = dateTime;
    }
}